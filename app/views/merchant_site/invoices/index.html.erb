<section class="merchant_invoices">
  <% @invoices.each do |invoice| %>
    <div id="invoice-<%= invoice.id %>">
      <%= link_to invoice.id, merchant_invoice_path(@merchant, invoice), method: :get %>
    </div>
  <% end %>
</section>