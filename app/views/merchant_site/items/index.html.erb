<h1> Merchant: <%= @merchant.name %> Items </h1>

<section class="name_of_items">
  <ul>
    <% @merchant.all_items.each do |item| %>
      <div id="item_block_<%= item.id %>">
        <li>
          <p> <%= link_to "#{item.name}", merchant_item_path(@merchant, item.id) %> </p>
          <%= button_to "Enable", merchant_items_path(@merchant) %> 
          <%= button_to "Disable", merchant_items_path(@merchant) %>
        </li>       
      </div>
    <% end %>
  </ul>
</section>